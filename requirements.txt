# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Itanta Agentic AI Framework
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Orchestration ────────────────────────────────────────────────────────────
langgraph>=0.2.0              # Agent graph, state machine, checkpointing, interrupt()
langgraph-checkpoint-sqlite   # SQLite checkpointer (separate package in langgraph >=0.2)
langchain-core>=0.2.0         # Base types used by LangGraph

# ── LLM Provider SDKs ────────────────────────────────────────────────────────
groq>=0.9.0                   # Groq API — Llama 3.1 70B + 8B (free tier)
google-generativeai>=0.7.0    # Gemini Flash + Pro (free tier)
httpx>=0.27.0                 # HTTP client for OpenRouter (DeepSeek Coder V2)

# ── Data validation ──────────────────────────────────────────────────────────
pydantic>=2.7.0               # State schema + all data contracts

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0          # Load .env API keys
PyYAML>=6.0.1                 # Parse config.yaml

# ── CLI + Terminal UI ─────────────────────────────────────────────────────────
typer>=0.12.0                 # CLI framework — NFR-01: 5-min setup
rich>=13.7.0                  # Rich terminal output — diffs, progress, tables

# ── Validators (deterministic — no LLM cost) ─────────────────────────────────
pytest>=8.2.0                 # TDD test runner
pytest-json-report>=1.5.0     # Structured JSON output from pytest
ruff>=0.4.0                   # Linter (replaces flake8 + isort + pycodestyle)
mypy>=1.10.0                  # Static type checker

# ── Utilities ────────────────────────────────────────────────────────────────
python-json-logger>=2.0.7     # Structured JSON activity log
gitpython>=3.1.43             # Git diff generation for diff_presenter

# ── Docker output (extended feature) ─────────────────────────────────────────
docker>=7.1.0                 # Python Docker SDK for compose generation + health check

# ── Generated project dependencies (written into generated projects) ──────────
# These are listed here so the dev environment can also run generated projects
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
alembic>=1.13.0
psycopg2-binary>=2.9.9        # PostgreSQL driver
motor>=3.4.0                  # Async MongoDB driver (Tier 5)
pymongo>=4.7.0                # Sync MongoDB (Tier 5)
python-jose[cryptography]>=3.3.0  # JWT (Tier 4)
passlib[bcrypt]>=1.7.4        # Password hashing (Tier 4)
httpx>=0.27.0                 # Async HTTP client (Tier 3 external APIs)
pytest-asyncio>=0.23.0        # Async test support for FastAPI
